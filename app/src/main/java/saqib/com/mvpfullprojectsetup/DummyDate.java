package saqib.com.mvpfullprojectsetup;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.Locale;


public class DummyDate {

   /* public static String getPrettyDate(String date) {

        SimpleDateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss", Locale.getDefault());
        try {
            Date converted = format.parse(date);
            PrettyTime prettyTime = new PrettyTime(Locale.getDefault());
            return prettyTime.format(converted);
        } catch (ParseException e) {
            e.printStackTrace();
        }
        return "Unknown Time";

    }*/
    // System.out.println("----** getPrettyDate() : "+getPrettyDate("2018-10-05 14:26:10"));
    //compile 'org.ocpsoft.prettytime:prettytime:4.0.1.Final'
}
